export enum HealthRecordStatus {
  SCHEDULED = 'scheduled',
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}

export const HealthRecordStatusEnum = {
  SCHEDULED: HealthRecordStatus.SCHEDULED,
  PENDING: HealthRecordStatus.PENDING,
  COMPLETED: HealthRecordStatus.COMPLETED,
  CANCELED: HealthRecordStatus.CANCELED,
};
