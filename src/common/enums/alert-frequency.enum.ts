export enum AlertFrequency {
  IMMEDIATE = 'immediate',
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
}

export const AlertFrequencyEnum = {
  IMMEDIATE: AlertFrequency.IMMEDIATE,
  DAILY: AlertFrequency.DAILY,
  WEEKLY: AlertFrequency.WEEKLY,
  MONTHLY: AlertFrequency.MONTHLY,
};
